    public int rob(int[] nums) {
        int notrob = 0;
        int rob = 0;
        for(int n:nums){
            int temp = notrob;
            notrob = Math.max(notrob,rob);
            rob = temp + n;
        }
    return Math.max(notrob,rob);
    }
